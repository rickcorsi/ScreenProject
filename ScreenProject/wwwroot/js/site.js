$('#colorbutton-blue').on('click', function () {
    if (document.fullscreenElement) {
        document.exitFullscreen();
    } else {
        $('#colorbutton-blue').get(0).requestFullscreen();
    }
});
$('#colorbutton-red').on('click', function () {
    if (document.fullscreenElement) {
        document.exitFullscreen();
    } else {
        $('#colorbutton-red').get(0).requestFullscreen();
    }
});
$('#colorbutton-green').on('click', function () {
    if (document.fullscreenElement) {
        document.exitFullscreen();
    } else {
        $('#colorbutton-green').get(0).requestFullscreen();
    }
});
$('#colorbutton-white').on('click', function () {
    if (document.fullscreenElement) {
        document.exitFullscreen();
    } else {
        $('#colorbutton-white').get(0).requestFullscreen();
    }
});
$('#colorbutton-black').on('click', function () {
    if (document.fullscreenElement) {
        document.exitFullscreen();
    } else {
        $('#colorbutton-black').get(0).requestFullscreen();
    }
});